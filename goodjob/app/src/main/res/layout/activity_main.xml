<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <FrameLayout
        android:id="@+id/containerFragments"
        android:layout_width="match_parent"
        android:layout_height="584dp"
        android:layout_alignBottom="@+id/navigation"
        android:layout_marginTop="80dp"
        android:layout_marginBottom="58dp">

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fabPublicarActividad"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/containerFragments"
            android:layout_gravity="end|bottom"
            android:layout_margin="10dp"
            android:clickable="true"
            app:srcCompat="@drawable/ic_add_to_activity_black_24dp" />
    </FrameLayout>


    <android.support.design.widget.BottomNavigationView
        android:id="@+id/navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#ccc"
        app:menu="@menu/navigation" />

    <ImageView
        android:id="@+id/ImgProfile"
        android:layout_width="58dp"
        android:layout_height="59dp"
        android:layout_alignParentRight="true"
        android:layout_marginTop="15dp"
        android:layout_marginRight="17dp"
        app:srcCompat="@mipmap/ic_launcher_round" />

    <EditText
        android:id="@+id/editText"
        android:layout_width="295dp"
        android:layout_height="wrap_content"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="20dp"
        android:ems="10"
        android:hint="Buscar"
        android:inputType="textPersonName" />

</RelativeLayout>